<div class="wk-mp-design">
    <fieldset class="fieldset info wk-mp-fieldset">
        <legend class="legend"><span>
                <?= __('Create New Ticket') ?>
            </span></legend>

        <form action="<?= $block->escapeUrl($block->getUrl('sellerticket/ticket/save')) ?>" method="post"
            id="form-ticket-new" class="form" data-mage-init='{"validation":{}}'>
            <fieldset class="fieldset">
                <?= $block->getBlockHtml('formkey') ?>

                <div class="field subject required">
                    <label class="label" for="subject"><span>
                            <?= __('Subject') ?>
                        </span></label>
                    <div class="control">
                        <input type="text" name="subject" id="subject" title="<?= __('Subject') ?>" class="input-text"
                            data-validate="{'required-entry':true}">
                    </div>
                </div>

                <div class="field category required">
                    <label class="label" for="category"><span>
                            <?= __('Category') ?>
                        </span></label>
                    <div class="control">
                        <select name="category" id="category" title="<?= __('Category') ?>" class="select"
                            data-validate="{'required-entry':true}">
                            <option value="">
                                <?= __('-- Select Category --') ?>
                            </option>
                            <option value="General Support">
                                <?= __('General Support') ?>
                            </option>
                            <option value="Billing">
                                <?= __('Billing') ?>
                            </option>
                            <option value="Technical/Bugs">
                                <?= __('Technical/Bugs') ?>
                            </option>
                            <option value="Feature Request">
                                <?= __('Feature Request') ?>
                            </option>
                        </select>
                    </div>
                </div>

                <div class="field priority required">
                    <label class="label" for="priority"><span>
                            <?= __('Priority') ?>
                        </span></label>
                    <div class="control">
                        <select name="priority" id="priority" title="<?= __('Priority') ?>" class="select"
                            data-validate="{'required-entry':true}">
                            <option value="Low">
                                <?= __('Low') ?>
                            </option>
                            <option value="Normal" selected="selected">
                                <?= __('Normal') ?>
                            </option>
                            <option value="High">
                                <?= __('High') ?>
                            </option>
                            <option value="Urgent">
                                <?= __('Urgent') ?>
                            </option>
                        </select>
                    </div>
                </div>

                <div class="field message required">
                    <label class="label" for="message"><span>
                            <?= __('Message') ?>
                        </span></label>
                    <div class="control">
                        <textarea name="message" id="message" title="<?= __('Message') ?>" class="input-text" cols="5"
                            rows="8" data-validate="{'required-entry':true}"></textarea>
                    </div>
                </div>

            </fieldset>

            <div class="actions-toolbar">
                <div class="primary">
                    <button type="submit" title="<?= __('Submit Ticket') ?>" class="action save primary">
                        <span>
                            <?= __('Submit Ticket') ?>
                        </span>
                    </button>
                </div>
                <div class="secondary">
                    <a class="action back" href="<?= $block->escapeUrl($block->getUrl('sellerticket/ticket/index')) ?>">
                        <span>
                            <?= __('Back to Tickets') ?>
                        </span>
                    </a>
                </div>
            </div>
        </form>
    </fieldset>
</div>