<?php
/** @var $block \Vendor\Marketplace\Block\Coupon\Index */
$coupons = $block->getCoupons();
?>
<div class="row">
    <div class="col-md-12">
        <div class="page-title-wrapper">
            <h1 class="page-title">
                <span class="base">
                    <?= __('My Coupons') ?>
                </span>
            </h1>
            <div class="actions-toolbar">
                <div class="primary">
                    <a href="<?= $block->getAddUrl() ?>" class="action primary">
                        <span>
                            <?= __('Add New Coupon') ?>
                        </span>
                    </a>
                </div>
            </div>
        </div>

        <?php if ($coupons && count($coupons)): ?>
            <div class="table-wrapper orders-history">
                <table class="data table table-order-items history" id="my-coupons-table">
                    <caption class="table-caption">
                        <?= __('Coupons') ?>
                    </caption>
                    <thead>
                        <tr>
                            <th scope="col" class="col id">
                                <?= __('Rule ID') ?>
                            </th>
                            <th scope="col" class="col date">
                                <?= __('Code') ?>
                            </th>
                            <th scope="col" class="col shipping">
                                <?= __('Rule Name') ?>
                            </th>
                            <th scope="col" class="col total">
                                <?= __('Discount') ?>
                            </th>
                            <th scope="col" class="col status">
                                <?= __('Status') ?>
                            </th>
                            <th scope="col" class="col actions">
                                <?= __('Actions') ?>
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($coupons as $coupon): ?>
                            <tr>
                                <td data-th="<?= __('Rule ID') ?>" class="col id">
                                    <?= $coupon->getRuleId() ?>
                                </td>
                                <td data-th="<?= __('Code') ?>" class="col date">
                                    <span class="status-badge active" style="min-width: unset;">
                                        <?= $block->escapeHtml($coupon->getCode()) ?>
                                    </span>
                                </td>
                                <td data-th="<?= __('Name') ?>" class="col shipping">
                                    <?= $block->escapeHtml($coupon->getName()) ?>
                                </td>
                                <td data-th="<?= __('Discount') ?>" class="col total">
                                    <?php
                                    $action = $coupon->getSimpleAction();
                                    $amount = (float) $coupon->getDiscountAmount();
                                    if ($action == 'by_percent') {
                                        echo $amount . '%';
                                    } else {
                                        echo '$' . number_format($amount, 2); // Should format currency properly
                                    }
                                    ?>
                                </td>
                                <td data-th="<?= __('Status') ?>" class="col status">
                                    <?php if ($coupon->getIsActive()): ?>
                                        <span class="status-badge success">
                                            <?= __('Active') ?>
                                        </span>
                                    <?php else: ?>
                                        <span class="status-badge error">
                                            <?= __('Inactive') ?>
                                        </span>
                                    <?php endif; ?>
                                </td>
                                <td data-th="<?= __('Actions') ?>" class="col actions">
                                    <a href="<?= $block->getEditUrl($coupon->getRuleId()) ?>" class="action view">
                                        <span>
                                            <?= __('Edit') ?>
                                        </span>
                                    </a>
                                    <a href="#" class="action delete"
                                        onclick="deleteCoupon('<?= $block->getDeleteUrl($coupon->getRuleId()) ?>')">
                                        <span>
                                            <?= __('Delete') ?>
                                        </span>
                                    </a>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
            <script>
                function deleteCoupon(url) {
                    if (confirm('<?= __('Are you sure you want to delete this coupon?') ?>')) {
                        window.location.href = url;
                    }
                }
            </script>
        <?php else: ?>
            <div class="message info empty"><span>
                    <?= __('You have created no coupons.') ?>
                </span></div>
        <?php endif; ?>
    </div>
</div>