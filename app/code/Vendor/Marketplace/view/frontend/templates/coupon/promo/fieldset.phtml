<?php
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 */

/**@var \Magento\Backend\Block\Widget\Form\Renderer\Fieldset $block */
/** @var \Magento\Framework\View\Helper\SecureHtmlRenderer $secureRenderer */
?>
<style>
    .rule-tree .fieldset {
        border: 0;
        padding: 0;
        margin-top: 10px;
    }

    .rule-tree .legend {
        font-size: 1.4rem;
        font-weight: 600;
        margin-bottom: 15px;
    }

    .rule-tree ul {
        list-style: none;
        padding-left: 30px;
        margin: 10px 0;
        border-left: 1px dashed #ccc;
    }

    .rule-tree li {
        margin: 8px 0;
        line-height: 24px;
        position: relative;
    }

    .rule-param {
        display: inline-block;
        vertical-align: middle;
    }

    .rule-param a.label {
        color: #1979c3;
        text-decoration: none;
        border-bottom: 1px dashed #1979c3;
        font-weight: 600;
        padding: 0 2px;
    }

    .rule-param a.label:hover {
        background: #e1effb;
    }

    .rule-param-edit {
        display: none;
    }

    .rule-param-children {
        margin-top: 5px;
    }

    .rule-param-remove {
        display: inline-block;
        width: 16px;
        height: 16px;
        background: #e22626;
        color: #fff;
        text-align: center;
        line-height: 14px;
        border-radius: 50%;
        font-size: 14px;
        margin-left: 8px;
        cursor: pointer;
        font-weight: bold;
        vertical-align: middle;
    }

    .rule-param-remove:before {
        content: '×';
    }

    .rule-param-add {
        display: inline-block;
        width: 16px;
        height: 16px;
        background: #79a22e;
        color: #fff;
        text-align: center;
        line-height: 16px;
        border-radius: 50%;
        font-size: 14px;
        margin-left: 8px;
        cursor: pointer;
        font-weight: bold;
        vertical-align: middle;
    }

    .rule-param-add:before {
        content: '+';
    }

    .rule-tree select:not([style*="display: none"]):not(.hidden) {
        width: auto;
        height: 28px;
        padding: 0 5px;
        display: inline-block;
        vertical-align: middle;
        margin: 0 5px;
        border: 1px solid #adadad;
        background: #fff;
        font-size: 13px;
    }

    .rule-tree input:not([type="hidden"]):not([style*="display: none"]):not(.hidden) {
        width: 150px;
        height: 28px;
        padding: 0 8px;
        display: inline-block;
        vertical-align: middle;
        border: 1px solid #adadad;
        font-size: 13px;
    }

    .rule-tree b {
        font-weight: 600;
    }
</style>
<?php $_element = $block->getElement() ?>
<?php $_jsObjectName = $block->getFieldSetId() != null ? $block->getFieldSetId() : $_element->getHtmlId() ?>
<div class="rule-tree">
    <fieldset id="<?= $block->escapeHtmlAttr($_jsObjectName) ?>" <?= /* @noEscape */ $_element->serialize(['class']) ?>
        class="fieldset">
        <legend class="legend"><span>
                <?= $block->escapeHtml($_element->getLegend()) ?>
            </span></legend>
        <br>
        <?php if ($_element->getComment()): ?>
            <div class="messages">
                <div class="message message-notice">
                    <?= $block->escapeHtml($_element->getComment()) ?>
                </div>
            </div>
        <?php endif; ?>
        <div class="rule-tree-wrapper">
            <?= $_element->getChildrenHtml() ?>
        </div>
    </fieldset>
</div>

<?php $scriptString = <<<script

require([
    'Magento_Rule/rules',
    'prototype'
], function(VarienRulesForm){

script;
$scriptString .= 'window.' . /* @noEscape */ $_jsObjectName . ' = new VarienRulesForm(\'' .
    /* @noEscape */ $_jsObjectName . '\', \'' .  /* @noEscape */ $block->getNewChildUrl() . '\');';
if ($_element->getReadonly()):
    $scriptString .= /* @noEscape */ $_element->getHtmlId() . '.setReadonly(true);' . PHP_EOL;
endif;

$scriptString .= '});' . PHP_EOL;
?>
<?= /* @noEscape */ $secureRenderer->renderTag('script', [], $scriptString, false) ?>